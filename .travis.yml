language: java

jdk:
  - oraclejdk8

branches:
  only:
    - travis

cache:
  directories:
    - $HOME/.m2

before_install:
  - echo "MAVEN_OPTS='-DcheckedArithmetic=true -Xmx1g'" > ~/.mavenrc
  - export TZ="America/Los_Angeles"
  - date

notifications:
  hipchat: HIPCHAT_TOKEN@Gosu
